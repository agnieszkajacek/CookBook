<!DOCTYPE html>
<html>
  <head>
    <title>CookBook</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div class="container">
      <div class="row">
        <div class="col-md-2">
          <ul class="nav nav-pills nav-stacked">
            <li role="presentation" class="dropdown">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                Menu <span class="caret"></span>
              </a>

              <ul class="dropdown-menu">
                <% Category.all.each do |category| %>
                <li><%= link_to category.name, category %></li>
                <% end %>
              </ul>
          <li role="presentation"><a href="#">Ulubione przepisy</a></li>
          </ul>
        </div>

        <div class="col-md-10">
          <p class="navbar-text pull-right">
      <% if user_signed_in? %>
        Zalogowany jako <strong><%= current_user.email %></strong>.
        <%= link_to 'Edytuj profil', edit_user_registration_path, :class => 'navbar-link' %> |
        <%= link_to "Wyloguj się", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
      <% else %>
        <%= link_to "Załóż konto", new_user_registration_path, :class => 'navbar-link'  %> |
        <%= link_to "Zaloguj się", new_user_session_path, :class => 'navbar-link'  %>
      <% end %>
        </div>
      </div>



      <div class="row">
        <div class="col-md-4">
          <form>
            <div class="form-group has-feedback">
              <input placeholder="Wyszukaj przepis..." type="text" class="form-control" id="inputSuccess2" aria-describedby="inputSuccess2Status">
              <span class="glyphicon glyphicon-search form-control-feedback" aria-hidden="true"></span>
            </div>
           </form>
        </div>
      </div>

    <% if notice %>
      <p class="alert alert-success"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-danger"><%= alert %></p>
    <% end %>

      <div class="row">
        <div class="col-md-7 col-md-offset-2"><%= yield %></div>
      </div>

    </div>
  </body>
</html>
